<tr
  data-row="{{ ident }}"
  {{#if duplicates}}data-duplicates="{{ duplicates }}"{{/if}}
>
  <th
    scope="row"
    id="{{ identId }}"
    headers="{{ groupId }}-ident"
    data-column="ident"
  >{{{ labelHtml }}}</th>

  {{{ cells }}}

</tr>